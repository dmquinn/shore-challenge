import React from "react";

const AddButton = () => {
  return (
    <button className="py-2 px-4 rounded-full border border-green text-green w-40 ml-10">
      Add
    </button>
  );
};

export default AddButton;
